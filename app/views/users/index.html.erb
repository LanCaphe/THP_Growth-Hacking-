
    <div class="container">
        <div class="cta-title">
            <h1>Inscrivez-vous<br/>à cette superbe newsletter</h1>
        </div>
        <div class="form-wrapper">
            <%= form_for @user do |form| %>
              <div class="field">
                <%= form.label :name %>
                <%= form.text_field :name %>
              </div>

              <div class="field">
                <%= form.label :email %>
                <%= form.text_field :email %>
              </div>

              <div class="actions">
                <%= form.submit %>
              </div>
            <% end %>
        </div>
    </div>
